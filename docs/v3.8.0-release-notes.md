# Unified Memory v3.8.x Release Notes
<!-- zh -->

**Current Version**: 3.8.0  
**Release Date**: 2026-03-30  
**Total Tools**: 112 (97 + 15 new)

> 本文档覆盖 v3.8.x 全部版本，当前为 v3.8.0。后续 v3.8.y 小版本更新将在本文档顶部追加。

---

## 🚀 Major Features

### 1. WAL Protocol (Write-Ahead Log)
<!-- zh -->

Complete Write-Ahead Log implementation for data durability and crash recovery.

**New Tools**:
- `memory_wal_write` - Write entry to WAL with checksum
- `memory_wal_replay` - Replay WAL entries for crash recovery
- `memory_wal_status` - Get WAL status and statistics
- `memory_wal_truncate` - Truncate WAL after successful commit
- `memory_wal_export` - Export WAL for backup
- `memory_wal_import` - Import WAL from backup

**Features**:
- ✅ Checksum verification for data integrity
- ✅ Automatic crash recovery on startup
- ✅ Export/import for backup and migration
- ✅ Backward compatible with existing storage.js

**Technical Details**:
- File: `src/wal.js` (~5.3KB)
- JSON-based WAL format (`.wal.jsonl`)
- Automatic cleanup on clean shutdown
- Crash recovery replays pending entries

**Usage Example**:
```bash
# Write to WAL
mcporter call unified-memory.memory_wal_write(
  operation: "insert",
  collection: "memories",
  data: { text: "Important memory", importance: 0.9 }
)

# Check WAL status
mcporter call unified-memory.memory_wal_status()

# Export for backup
mcporter call unified-memory.memory_wal_export()
```

---

### 2. Evidence Chain Mechanism
<!-- zh -->

Track the source and evolution of memories through evidence chains.

**New Tools**:
- `memory_evidence_add` - Add evidence to memory's chain
- `memory_evidence_get` - Get evidence chain for a memory
- `memory_evidence_find_by_type` - Find memories by evidence type
- `memory_evidence_find_by_source` - Find memories by source ID
- `memory_evidence_stats` - Get evidence statistics

**Evidence Types**:
| Type | Description |
|------|-------------|
| `transcript` | From conversation transcript |
| `message` | From specific message |
| `manual` | Manually added |
| `inference` | AI inferred |
| `git_note` | From Git note |
| `revision` | From revision |

**Features**:
- ✅ Source tracking (transcript/message/manual/inference)
- ✅ Confidence scoring (0-1)
- ✅ Evidence chain visualization
- ✅ Source-based filtering and search

**Technical Details**:
- File: `src/evidence.js` (~5.7KB)
- SQLite-like evidence storage
- Per-memory evidence chains
- Confidence-weighted recall

**Usage Example**:
```bash
# Add evidence
mcporter call unified-memory.memory_evidence_add(
  memoryId: "mem_123",
  type: "transcript",
  sourceId: "msg_456",
  confidence: 0.95,
  context: "User mentioned preference for dark mode"
)

# Get evidence chain
mcporter call unified-memory.memory_evidence_get(memoryId: "mem_123")

# Get evidence stats
mcporter call unified-memory.memory_evidence_stats()
```

---

### 3. Auto Organization
<!-- zh -->

Automatic memory tiering and compression across HOT/WARM/COLD tiers.

**New Tools**:
- `memory_organize` - Organize memories across tiers
- `memory_compress_tier` - Compress memories in a specific tier
- `memory_archive_old` - Archive memories older than threshold
- `memory_tier_stats` - Get tier statistics
- `memory_full_organize` - Run full organization (organize + compress + archive)

**Tier Configuration**:
| Tier | Max Age | Max Memories | Compression |
|------|---------|--------------|-------------|
| HOT | 7 days | 10 | 50% |
| WARM | 30 days | 50 | 30% |
| COLD | 365 days | 1000 | 10% |

**Features**:
- ✅ Automatic tier migration based on age and importance
- ✅ Memory compression (summarization)
- ✅ Archive old memories (365+ days)
- ✅ Tier-specific compression ratios
- ✅ Statistics and monitoring

**Technical Details**:
- File: `src/organize.js` (~7.9KB)
- Tier-based memory management
- Importance-weighted retention
- Archive for long-term storage

**Usage Example**:
```bash
# Full organization
mcporter call unified-memory.memory_full_organize()

# Get tier stats
mcporter call unified-memory.memory_tier_stats()

# Archive old memories
mcporter call unified-memory.memory_archive_old(thresholdDays: 365)
```

---

## 📊 Statistics

| Metric | Before (v3.7.0) | After (v3.8.0) | Change |
|--------|-----------------|-----------------|--------|
| Total Tools | 97 | 112 | +15 |
| Lines of Code | ~25K | ~29K | +4K |
| New Files | - | 3 | +3 |
| Data Durability | Basic | WAL Protocol | ✅ |
| Evidence Tracking | None | Full Chain | ✅ |
| Auto Organization | Manual | Automatic | ✅ |

---

## 🔧 Technical Details

### New Files Added in v3.8.0

| File | Size | Description |
|------|------|-------------|
| `src/wal.js` | ~5.3KB | WAL Protocol implementation |
| `src/evidence.js` | ~5.7KB | Evidence Chain implementation |
| `src/organize.js` | ~7.9KB | Auto Organization implementation |

### WAL Protocol Implementation
<!-- zh -->

- Compatible with existing `storage.js` and `tier.js`
- Checksum verification using simple hash
- JSON-based WAL format (`.wal.jsonl`)
- Automatic crash recovery on startup
- Export/import for backup and migration

### Evidence Chain Implementation
<!-- zh -->

- SQLite-like evidence storage in `data/evidence.db`
- Confidence scoring system (0-1)
- Source-based filtering and search
- Statistics and analytics
- Evidence chain per memory

### Auto Organization Implementation
<!-- zh -->

- Tier-based memory management
- Importance-weighted retention
- Automatic compression (summarization)
- Archive functionality for old memories
- Statistics and monitoring endpoints

---

## 📝 Migration Guide

### From v3.7.0 to v3.8.0
<!-- zh -->

**No breaking changes** — All existing tools remain functional.

**Recommended Actions**:
1. Enable WAL protocol for critical data (enabled by default)
2. Start tracking evidence for important memories
3. Run `memory_full_organize` to optimize memory tiers

**Configuration** (Optional):
```bash
# Enable WAL (enabled by default)
export ENABLE_WAL=true

# Configure evidence tracking
export EVIDENCE_TRACKING=true

# Configure auto-organization schedule
export AUTO_ORGANIZE_INTERVAL=86400  # 24 hours
```

---

## 🎯 Roadmap

### v3.9.0 (Planned - 2026-04-21)
- [ ] Transcript-First Architecture
- [ ] Revision Lifecycle Deepening
- [ ] Lane Memory Deep Integration
- [ ] Token Budget Hard Limits
- [ ] Evidence-Driven Recall

### v4.0.0 (Planned - 2026-05-05)
- [ ] SuperMemory API Integration
- [ ] Knowledge Graph Enhancement
- [ ] Multi-Agent Collaboration
- [ ] Memory Quality Assessment

---

## 🙏 Acknowledgments

Inspired by:
- **Elite Longterm Memory** — WAL protocol design
- **Smart Memory** — Evidence chain mechanism
- **Memory Tiering** — Auto organization workflow

---

## 📄 License

MIT — Free to use, modify, and redistribute.

---

**Full Changelog**: https://github.com/mouxangithub/unified-memory/compare/v3.7.0...v3.8.0
